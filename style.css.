/* Police et couleurs générales */
body {
  font-family: Arial, sans-serif;
  margin: 0;
  padding: 0;
  background: #f3f3f3;
  color: #333;
}

/* Header */
header {
  background: #1d3557;
  color: white;
  padding: 30px 20px;
  text-align: center;
}

.subtitle {
  margin-top: 10px;
  font-size: 1.1rem;
  opacity: 0.9;
}

/* Conteneur du quiz */
#quiz-container {
  max-width: 800px;
  background: white;
  margin: 30px auto;
  padding: 20px 30px;
  border-radius: 10px;
  box-shadow: 0 5px 10px rgba(0,0,0,0.1);
}

/* Questions */
.question {
  margin-bottom: 25px;
}

.question h3 {
  margin-bottom: 10px;
}

/* Choix */
.option {
  background: #e9ecef;
  padding: 12px 15px;
  margin: 6px 0;
  border-radius: 8px;
  cursor: pointer;
  transition: 0.2s;
}

.option:hover {
  background: #d7e3ff;
}

.option.selected {
  background: #a8dadc;
}

/* Boutons */
button {
  padding: 12px 20px;
  font-size: 1.1rem;
  border: none;
  background: #457b9d;
  color: white;
  border-radius: 10px;
  cursor: pointer;
  margin: 20px auto;
  display: block;
  transition: 0.2s;
}

button:hover {
  background: #1d3557;
}

/* Zone résultat */
#result-container {
  max-width: 800px;
  background: white;
  margin: 30px auto;
  padding: 30px;
  border-radius: 10px;
  box-shadow: 0 5px 10px rgba(0,0,0,0.1);
}

#result-title {
  font-size: 2rem;
  color: #1d3557;
  margin-bottom: 15px;
}

#result-text {
  font-size: 1.1rem;
  line-height: 1.6;
}
